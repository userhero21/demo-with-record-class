server:
  port: 8888

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/demorecordclass
    username: postgres
    password: 123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false